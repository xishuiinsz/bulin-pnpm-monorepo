{
  "extends": "../../tsconfig.base.json", // 继承根目录配置文件
  "compilerOptions": {
    // 编译选项
    "paths": {
      // 设置应用别名
      "package1": ["./src/index.ts"],
      "package1/*": ["./src/*.ts"],
    },
  },
}
